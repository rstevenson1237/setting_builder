---
name: <The setting's name. Two or three words, decomposing into recorded roots.>
tags: [<three>, <thematic>, <tags>]
seed: <integer>
containers:
  - id: <lowercase-slug>
    name: <The container's name>
schema_version: 1
---

## Overview

<Player register. What a person standing anywhere in this setting sees, hears
and is walking on. Shows, never concludes, carries no token. Three or four
paragraphs, and every noun in it is true in every region.>

## Style

<Referee register. How this setting's prose sounds and what it counts in: the
units, the naming layers, the register the tables are written to. This is the
page a writer of a later step reads before writing anything.>

## Tables

<Generated by `scaffold.py` from the catalogue in SPEC.md section 4.7. The
twenty-four rows are structure, not content, and no step rewrites them.>

## Regions

<The roster, written at step 2. Placement only: code, name, type and
setting-level container. Difficulty and weight are dials and live in each
region's own frontmatter, where they cannot drift from the region they size.>

<!-- DIAGRAM: T1_SETTING.md -->

<!-- DIAGRAM: T2_<CONTAINER>.md -->
