# The tells
#
# A tell is the signature of a class of failure - a candidate to read, never an
# error. `STYLE.md` is the authority on what one is and how it is cleared, and
# `INTROSPECTIVE.md`'s Baseline metrics on what each is calibrated against. This
# file is the list: `tools/validate_setting.py` reads it, and `python3
# tools/metrics.py --tells [PATH]` reports it over any markdown.
#
# One signature per line, four fields:
#
#   key | unit | rule | pattern
#
# key      what the report calls it. Two lines may share a key and their hits
#          are one count, so one unit matching both is one hit.
# unit     what the pattern is matched against. `line` is the whole line with
#          its citations stripped, and every match is a hit. `sentence` is one
#          hit per sentence. `feature-opening` is a Feature's opening clause,
#          and fires only where that clause also names a word of its own label.
# rule     what the tell is checking, in the words the report prints. Every line
#          under one key states it identically.
# pattern  a case-insensitive regex, taken whole and so free to hold `|`. Parts
#          joined by ` && ` must all match the same unit. `{name}` draws a
#          vocabulary list defined above it by `= name | pattern`, and a
#          vocabulary named again extends it.
#
# One line is not a signature. `motif | setting | value` configures the motif
# tell, which is counted over the corpus rather than matched against a unit and
# is specified at the foot of this file.
#
# Two obligations come with a line, and `python3 tools/validate_setting.py
# --fixtures` reports them:
#
#   fixtures/bad/<key>.md   required, the key hyphenated: a known-bad entry this
#                           tell fires on. A tell with none is a pattern nothing
#                           keeps honest.
#   fixtures/good/<key>.md  optional: the near misses, an entry carrying the
#                           vocabulary legitimately that this tell must not fire
#                           on. What a tell does not fire on is shown there
#                           rather than written here.
#
# `style/exemplars/` is the good corpus for every tell at once and stays at
# zero, so a pattern that fires on the endorsed register is measuring the wrong
# thing and is cut or narrowed rather than argued with.
#
# Precision is uneven and deliberately so. `rather than` is exact. The other
# three flag the shape a failure takes, and whether a given hit is that failure
# is a reading.

= neg   | \b(nothing|nobody|no one|no-one|none|never|nowhere|not once|no longer)\b
= reach | \b(anywhere|elsewhere|else|in living memory|in years|for generations|for centuries|ever|since|longer than|of its kind)\b
= idiom | \b(the only|only one|matching nothing|unlike anything)\b

rather than     | line            | the trailing clause, per PR #41 | \brather than\b
absence claim   | sentence        | absence across time or space, per STYLE.md | {idiom}
absence claim   | sentence        | absence across time or space, per STYLE.md | {neg} && {reach}
conclusion tell | line            | the players' conclusion written down, per GENRE.md | \b(which is (?:why|how|what)|the reason\b|proof\b|recognis|recogniz|unmistakab|obviously|clearly|evidently|meaning that|means that|explains\b|suggest|implie|implying|will (?:find|know|realise|realize|notice|understand|remember)|enough to tell|tells anyone|anyone can tell|the clue that|so that anyone|is how anyone)
gloss           | feature-opening | a term carrying its own definition, per Location.md | ^(the|a|an|its|this)\b

# The motif tell
#
# The one tell that is counted rather than matched. A motif is not dense inside
# a region - it has spread across them - so a per-region density count misses it
# and is swamped by the vocabulary every room needs. What is counted is, for
# each content word of a keyed location, the rooms it appears in and the regions
# those rooms sit in. A word over both thresholds is a candidate to read, never
# an error: a recurring object is how a setting holds together, and a recurring
# word across most of the map is how its voice narrows. Which of the two a
# given hit is, is a reading.
#
# The second count runs the same reading over `setting/*.md` and the region
# overviews, by occurrence: the seed, what the context is putting in a
# generator's mouth before a room is written.
#
# Four lines configure it, and `motif` is therefore not available as a tell key:
#
#   motif | regions | N   report a word appearing in N regions or more
#   motif | rooms   | N   and in N rooms or more
#   motif | seed    | N   report a setting-level word at N occurrences or more
#   motif | ignore  | a pattern; a word it matches whole is never a motif
#
# A word of two letters or fewer, and a word the validator's own stopword list
# holds, is never a motif either. What `ignore` adds is the vocabulary an
# entry's shape requires - the citation machinery, the measures, the bearings,
# the room's own furniture, the condition words the lists supply - and the
# general language that stopword list does not reach. A vocabulary named again
# extends it, so a long list is several lines.
#
# The Bestiary, faction and Language.md names are read from the setting rather
# than listed here, since a setting that renames its creatures would otherwise
# report every new name. A name of one word is excluded; a name of several is
# not, because its words are vocabulary the setting also uses elsewhere.

= machinery | exits?|lore|keys?|quests?|treasure|named|creatures?|unique|bestiary|tests?
= machinery | blocks?|purpose|regions?|rooms?|locations?|tags?|summary|notes?|features?
= machinery | description|range|sign|disposition|special|tiers?|tables?|rolls?|rolled
= machinery | counts?|actions?|boundary|costs?|stated|part(?:y|ies)|referee|players?
= machinery | characters?|settled|handles?|left|right|iii|vii|viii|cn|sp|gp|ad|ma|hd|hp
= machinery | dice|die|piercing|crushing|poison|fire|frost|blast|constitution|sanity|fate
= machinery | settings?

= measure | feet|foot|yards?|inch|inches|miles?|paces?|spans?|across|wide|width|high
= measure | heights?|deep|depths?|long|lengths?|tall|thick|thin|narrow|narrower|wider
= measure | deeper|longer|shorter|taller|half|quarters?|thirds?|dozen|score|hundred
= measure | thousand|eleven|twelve|thirteen|fourteen|fifteen|sixteen|seventeen|eighteen
= measure | nineteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|square

= bearing | north|northern|south|southern|east|eastern|west|western|above|below|beneath
= bearing | behind|beyond|inside|outside|upwards?|downwards?|near|nearer|far|farther
= bearing | further|cent(?:re|er)|middle|sides?|edges?|ends?|top|bottom|front|back
= bearing | corners?|opposite|along|past|through|around|between|under|over|towards?
= bearing | facing|beside|overhead|underfoot

= shell | chambers?|walls?|floors?|ceilings?|roofs?|doors?|doorway|stairs?|stairway|steps?
= shell | passages?|corridors?|openings?|entrance|gates?|gateway|arch|arches|archway
= shell | thresholds?|mouth|shaft|ledge|ground|surface|space|halls?|court|courtyard
= shell | alcove|recess

= condition | worn|wears?|wear|broken|breaks?|break|cuts?|cut|empty|full|bare|clean|clear
= condition | fresh|flat|plain|loose|tight|dry|wet|cold|warm|heavy|rough|smooth|hard|soft
= condition | sounds?|smells?|smelling|young

= common | stands?|standing|sits?|sitting|runs?|running|carr(?:y|ies|ied|ying)|holds?
= common | holding|held|opens?|opened|closes?|closed|sets?|takes?|taken|gives?|given
= common | goes?|going|gone|comes?|coming|keeps?|kept|leaves?|leaving|makes?|making|made
= common | puts?|shows?|showing|shown|names?|naming|works?|worked|working|marks?|marking
= common | marked|builds?|building|built|reach(?:es|ed)?|moves?|moved|lies|lying|rests?
= common | resting|means?|meaning|meant|wants?|needs?|finds?|found|sees?|seen|knows?
= common | known|says?|said|tells?|told|gets?|got|lets?|turns?|turned|uses?|used|pulls?
= common | push(?:es|ed)?|fits?|fitted|sends?|sending|sent|brings?|brought
= common | what|who|whom|whose|whatever|whoever|whichever|never|ever|once|else|until
= common | since|about|already|yet|still|whether|because|can|cannot|could|will|would
= common | shall|should|may|might|must|per|ways?|times?|days?|years?|things?|person
= common | people|someone|anyone|everyone|whole|without|within|exactly|actually|enough
= common | instead|though|although|unless|while|during|again|always|often|rarely|usually
= common | sometimes|now|then|true|false|another|others|either|neither|less|least|single
= common | before|after|against|straight|free|something|anything|hands?|heads?|eyes?|eye
= common | arms?|faces?|lines?|seconds?|third|his|hers?|him|ours?|theirs|too|nobody
= common | somebody|everybody

motif | regions | 3
motif | rooms   | 5
motif | seed    | 24
motif | ignore  | {machinery}|{measure}|{bearing}|{shell}|{condition}|{common}
