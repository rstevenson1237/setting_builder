# Setting Judgement Check

Cleared. The prior contents of this file were a full pass over "Brackvaen," a setting that
no longer exists in `setting/` (cleared for fresh generation in an earlier commit, along
with two later resets). Leaving that write-up in place after the content it described was
gone made it stale documentation and, worse, the single most complete exemplar of a
salt/debt/tide-bond motif that three independent from-scratch generations converged on -
see the Constraints entry in `patterns/setting/Setting.md` for the diagnosis.

Per `STEPS.md` step 5c, this file is (re)created once the current setting's regions and
locations are complete, following `templates/Setting_Judgement_Check.md`, and reads broadly
across every level since cross-level coherence is what's being judged.
</content>
